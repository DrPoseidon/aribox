const colorModelRouter = require('./colorModel-routes');
const commonImageRouter = require('./commonImage-routes');
const productRouter = require('./product-routes');
const sizeRouter = require('./size-routes');
const apiRouter = require('./api-routes');

module.exports = {
  colorModelRouter,
  commonImageRouter,
  productRouter,
  sizeRouter,
  apiRouter
}
