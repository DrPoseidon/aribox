const colorModel = require('./colorModel-model');
const commonImage = require('./commonImage-model');
const product = require('./product-model');
const size = require('./size-model');

module.exports = {
  colorModel,
  commonImage,
  product,
  size
}
